(function () {
  'use strict';

  angular.module('metronome.services.transpiler', [])
    .factory('transpiler', ['http', function (http) {

      var scope = {
        task: function (task) {

        }
      };

      return scope;
    }]);
})();
